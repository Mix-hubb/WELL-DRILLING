-- ============================================================
-- 0010: ยกเลิกฟีเจอร์สลิปโอนเงิน (payment_slips) — ไม่ใช้งานแล้ว
-- ลบตาราง payment_slips และ index ที่เกี่ยวข้องทั้งหมด
-- ============================================================

DROP TABLE IF EXISTS public.payment_slips;
